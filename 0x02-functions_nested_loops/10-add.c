#include "main.h"
/**
 *add- adds two integers and returns the result
 * @a: int type, first parameter
 * @b: int type, second parameter
 * Return: the sum of a+b
 */
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}

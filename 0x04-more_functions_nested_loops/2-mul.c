#include "main.h"
/**
 * mul- multiplies two integers
 *@a: first int to be multiplied
 *@b: second int to be multiplied
 * Return: a*b
 */

int mul(int a, int b)
{
int result;

result = a * b;
return (result);
}
